{
  "compilerOptions": {
    "target": "ESNext", // 타입스크립트 컴파일 후 보여주는 자바스크립트 파일 버전
    "module": "ESNext", // 글로벌 모듈
    "outDir": "dist", // 컴파일 후 생기는 자바스크립트 파일이 어디에 위치했으면 좋겠는지 설정 (dist라고 설정하여 dist 폴더 안에 자바스크립트 파일이 생기게 됨)
    "strict": false, // 타입스크립트가 타입을 검사할 때 : 엄격한 검사 모드 (추론이 되지 않는 함수를 명시하지 않으면 오류 발생)

    // 타입스크립트는 프로젝트 안에 있는 모든 파일을 같은 모듈로 취급하기 때문에 다른 파일이라도 같은 변수명을 사용할 수 없다 (하지만 파일을 분리해놓게 의미가 없기 때문에 이를 해결하는 방법은 두가지가 있다)
    // 에러 해결 방법 2가지 ( export 활용, 옵션 설정 )

    "moduleDetection": "force" // 옵션 설정 / 모든 타입스크립트 파일을 개별 모듈로 취급하도록 설정
  },
  "ts-node": {
    "esm": true
  },
  "include": ["src"] // src 안에 있는 파일을 전부 컴파일 해라
}

// number
let num1: number = 123; // 음수, 양수 모두 포함
let num2: number = Infinity; // 숫자의 무한대를 가리키는 특수 숫자도 가능
let num3: number = NaN; // not a number

// string
let str1: string = "hello";
let str2: string = `hello ${num1}`;

// boolean
let bool1: boolean = true;
let bool2: boolean = false;

// null
let null1: null = null;

// undefined
let unde1: undefined = undefined;

// 리터럴 타입
// 직접 정의 가능 , 타입을 10이라고 정의했기 때문에 다른 값 넣을 수 없음
let numA: 10 = 10;
let strA: "hello" = "hello";
let boolA: true = true;
